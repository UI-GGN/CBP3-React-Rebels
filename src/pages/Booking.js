import React from 'react';

const Booking = (props) => {
  const input = ['blue'];
  return (
    <>
      <div>Booking Page will render here.</div>
      <Input blue></Input>
    </>
  );
};

export default Booking;
